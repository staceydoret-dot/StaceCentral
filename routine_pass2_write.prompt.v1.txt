PASS 2 OF 2 — WRITE ONLY. You do no searching. Pass 1 ("Job search — pass 1 of 2: search", trig_01KdKj7M4XyK7KWxy37qXFTw) ran four hours ago and left its results in a project file. Your whole job is to move that file onto Stacey's plan page, carefully, and tell her one thing. Keep this run short — the two passes exist because the old combined routine was abandoned mid-run after six hours.

STEP 1 — Read `claude/next_rows.json`.
- If it does not exist, or does not parse as JSON, or its `generated` field is not TODAY'S date: STOP. Publish nothing. Tell her in one line that today's search did not finish, so her board still shows the last good set — and that nothing is broken. Do not improvise rows, do not search, do not touch the page.
- Otherwise validate before going further: every row has `s`, `role`, `employer`; every `s` is "open", "working" or "gated"; "open" rows come first; at most 14 rows; every actual job carries integer `fit` and `ats` (training programmes and credential paths legitimately omit both). If validation fails, STOP, publish nothing, and say in one line what was malformed.

STEP 2 — Read the page. Artifact action "read" on https://claude.ai/code/artifact/b5d65276-7b55-4114-a415-3205e0c128eb, IMMEDIATELY before building the edit. Build only from the file that read saves. Other routines and Stacey herself also write this page, so a stale base will be refused.

STEP 3 — Edit exactly one key. The state is the JSON inside the `<script id="app-state">` tag. That tag string appears twice: the real block is the one followed by `{`; the other is a decoy inside a JavaScript template. Parse the real one.
- Replace ONLY `STATE.jobs.updated` (today), `STATE.jobs.note` (from the file) and `STATE.jobs.rows` (from the file). Set `STATE.updated` to today.
- PRESERVE BYTE-FOR-BYTE: `STATE.jobs.docs` — her neurodiagnostic resume, full CV and BLS card as embedded PDFs plus preview images. Deleting it deletes her documents. It must hold exactly three entries with ids `resume`, `cv`, `bls`. Company-specific tailored resumes are never kept here; if one appears, something is wrong — stop and say so.
- PRESERVE BYTE-FOR-BYTE: `STATE.jobs.fitNote` and `STATE.jobs.searchTerms`.
- Touch NOTHING else. `pipeline`, `today`, `wins`, `checked`, `notes`, `display`, `phases`, `therapy` and `pinned` are HERS. Do not touch the HTML, the styles or the scripts.

STEP 4 — Verify by RUNNING the checks in a script, not by reasoning about them: exactly one real app-state block; the JSON parses; `jobs.docs` has ids resume/cv/bls, each with non-empty `b64`, and is identical to what you read; `jobs.fitNote` and `jobs.searchTerms` identical to what you read; every top-level key other than `jobs` and `updated` byte-identical to what you read; only the app-state line differs from the source file. Fix and re-check on any failure. Do not publish a file that fails.

STEP 5 — Publish. The saved file starts with the platform's frame-runtime skeleton (`<!doctype html><html><head>…</head><body>`) and her content follows inside its body. Publish ONLY her content — everything after that skeleton's `<body>`, up to but not including the closing `</body></html>` — with no doctype, html, head or body tags of your own. Her content legitimately contains the string `<!doctype html>` inside a JavaScript template in `<script id="app-code">`; that is not a wrapper, leave it alone. Republish to that same url with label "jobs <date>". Do not pass `capabilities`. NEVER pass force. On a version conflict, re-read the newer version and redo the edit on top of it — someone else's work is in that version.
If the read or the publish fails, do not force and do not create a new artifact. Say in one line that the page could not be updated today; the rows stay in `claude/next_rows.json` for tomorrow.

STEP 6 — Append one dated line to `claude/sweep_log.md` (create it if absent): the date, whether you published, the row count, and anything that went wrong. Project docs have no append operation, so re-read the file and rewrite it whole. Keep it under 40 lines — drop the oldest entries. Do not touch `claude/debug.log`.

REPORT BACK in three lines or fewer, and make them count — this is the only message she gets from the daily search. Lead with the `action` field from `next_rows.json`: ONE thing she could do today, never a list. No counts, no percentages, no guilt wording, no "overdue" or "carried over". If nothing reachable changed, say that plainly — a quiet day is a fine answer. She has ADHD and a long commute; a wall of text does not get read. Do not ask clarifying questions; this run is unattended.