DAILY JOB SEARCH — ONE ROUTINE, SEARCH THEN PUBLISH. You search (STEPS 1–5), then you publish the result onto Stacey's plan page yourself (STEPS 7–10). There is no second routine any more: the two-pass version failed every day because the passes ran in different Claude Projects and could not see each other's files, and a Google Drive handoff stalls at a permission prompt (any writing MCP tool does, in a routine). Keep this run tight — the old single routine was ABANDONED mid-run on 2026-09-06 after six hours, and the rules below exist so that cannot recur: no web tools, Indeed only, four sweeps, then publish.

CONNECTOR RULE — use only the tools the original version of this routine used: the Indeed connector, the project files and the Artifact tool. Do NOT call Google Drive or any other connector that writes — on 2026-09-11 a Drive create_file call stalled the run at a permission prompt after the search had finished. Do NOT call the ZipRecruiter connector: it is not granted to this routine, and on 2026-09-10 and 2026-09-11 the run froze at a permission prompt for it within 30 seconds and never wrote `claude/next_rows.json`, so nothing reached her board either day. WebFetch and WebSearch are NOT usable here either: this routine has no approval for them, so ONE call stalls the whole run at a permission prompt until it is abandoned — exactly what happened at 16:17 UTC on 2026-09-11 (aset.org and a Workday page) and on 2026-09-10. NEVER call WebFetch or WebSearch. Wherever a step below says fetch, quote or re-check a page, use the Indeed connector (search_jobs / get_job_details) instead; where Indeed cannot reach it, carry the existing row and note forward unchanged and say so in `why`. STEP 5 is not optional, and its `generated` field must be today's date.

CANDIDATE STATUS — read before searching.
- NO R. EEG T. / RPSGT registry, NO CAAHEP graduation. That is the binding constraint. Roles gated on either are unreachable, not merely hard.
- Holds AHA BLS (issued 8/29/2026, renew 08/2028).
- Cleveland Clinic Patient Service Specialist since 3/17/2025. Corrective Action Step 2 dated 8/12/2026 bars her from ANY internal Cleveland Clinic application until 2026-11-12. Internal CC rows are "working", never "open". External employers unaffected.
- She applied to EEG Technician TRAINEE postings at Memorial, Nicklaus, HCA Florida and Baptist over months with ZERO interviews — never reached a human. Never present those as fresh leads. Cold portal applications are a failing channel; weight toward anything that bypasses the portal.
- Home ZIP 33021 (Hollywood). Memorial Regional and Joe DiMaggio are the best geography.
- University of Miami / UHealth has no apprenticeship but posts entry-rung IOM (intraoperative monitoring) Technologist 1 roles hiring on a life-sciences degree rather than a registry. Check UHealth every run. CONFIRMED 2026-09-10: UM's "EEG Technician 1, Full Time" (careers.miami.edu job R100090174, $52,500-$80,500) states "High School Diploma or equivalent/relevant experience, certification or license" and "Minimum 1 year of relevant experience" — no CAAHEP programme, no ABRET registry. It is the only local EEG posting found whose stated bar she already clears; carry it until it is filled or she is rejected. UM's "IOM Technologist 1" (R100098749, $46,000-$69,500) was open the same day.
- 2026-09-05: applied to Neurology Technician at The Neurologic Wellness Institute, Boca Raton. That row is "working". Follow-up call due 11 September if no reply.

STEP 1 — Read the page first, then three files. Artifact action "read" on https://claude.ai/code/artifact/b5d65276-7b55-4114-a415-3205e0c128eb; parse `STATE.jobs.rows` from the real `<script id="app-state">` block (the one followed by `{`; the other is a decoy in a JavaScript template). Those rows are part of the system of record: rows on the page that you cannot prove gone or filled CARRY FORWARD with their existing lane, scores and text — never drop a row just because today's tools could not re-check it. Then read `claude/job_board_state.md` (the other half of the record — do not re-discover from scratch), `claude/debug.log` (standing instruction changes at its end — READ ONLY, you no longer rewrite it), and `claude/link_checks.md` (written by the hourly link checker, trig_01HgmkMc4xZG4GmRa9NvBfY8). You own everything link_checks.md flags: confirm each SUSPECT/DEAD entry yourself, then retire the row with a reason and today's date, or replace the URL, or clear the flag — and remove the hourly job's chip ("Link may be down — call instead" / "This posting is gone — do not tap") once resolved. Note in link_checks.md which entries you resolved.

STEP 2 — Verify links, but only where it pays. Quote the requirement line (from Indeed get_job_details — never WebFetch) for: every row currently marked "open", and every row flagged in link_checks.md. Everything else carries forward on its existing note. A posting that still renders does NOT prove the link works — career sites serve cached copies of dead requisitions (proven 2026-09-03 on the Nicklaus requisition). Corroborate each deep requisition link against the Indeed connector; if they disagree, say so on the card rather than asserting it is live. When Stacey reports a dead link she is right — retire it that day. NEVER publish a `to.indeed.com/...` short link; they expire. Prefer employer careers pages and phone numbers over deep requisition URLs. Gone or filled rows move to Retired with a reason and today's date — never silently deleted.

STEP 3 — Search. Four Indeed sweeps, not seven. Broward first, then Miami-Dade; exclude West Palm Beach; Boca Raton allowed with an explicit Palm Beach County flag; exclude care-coordinator and behavioral-health-coordinator roles.
A. EEG Technician (non-registered); EEG Tech Trainee/Apprentice; Neurodiagnostic Technician trainee; Polysomnographic/Sleep Technician trainee — WEIGHT SLEEP UP, overnight shifts suit her (her attendance problem is specifically morning lateness); intraoperative neuromonitoring, FLORIDA EMPLOYERS FIRST (UM/UHealth, Baptist, Memorial, HCA, Nicklaus, Assure, Sentient, NuVasive Clinical Services, Neuro Alert, Accurate Monitoring) because IONM hires on a bachelor's — entry rungs only ('Technologist 1', trainee, associate), never Level 2 or CNIM-required, and flag early starts, call, travel and any training-repayment agreement honestly; exclude anything requiring relocation; CLINICAL RESEARCH COORDINATOR and research assistant roles in any therapeutic area (bachelor's plus research experience, weekday hours, no call, reads well to a medical school — she has two research assistantships); PSYCHOMETRIST and neuropsychology technician; TMS / transcranial magnetic stimulation technician and INTERVENTIONAL PSYCHIATRY technician — psychiatry clinics and hospital neuroscience institutes, hires on a bachelor's, weekday days without call; MEDICAL / HEALTHCARE INTERPRETER — Haitian Creole first, then Spanish: she is advanced in Creole, already does interpreter coordination in the Cleveland Clinic role, and Broward pays unusually well for it; NEUROLOGY PRACTICE roles (neurology technician, neurology medical assistant) where small practices answer a phone rather than a portal.
B. Paid apprenticeship/trainee programmes, often not posted as jobs: search Indeed for "apprentice" and "trainee" at Broward and Miami-Dade health systems incl. UM/UHealth. apprenticeship.gov and Florida DOE listings cannot be fetched from this routine — carry their existing rows forward unchanged. Record COHORT START and APPLICATION WINDOW separately.
C. Credential paths — the door needing nobody's permission. The CAAHEP programme list (aset.org) cannot be fetched from this routine. Carry the programme rows in job_board_state.md forward unchanged (Pennsylvania Institute of Technology, Institute of Health Sciences, Labouré; none in Florida); do not re-check cost, start dates or WIOA funding here — that is for an interactive session. She has delinquent federal loans — funding matters as much as the programme, so keep that caveat on the rows.
D. Standing checks (Indeed search only): whether an EEG Technician Apprentice requisition has opened at Cleveland Clinic Weston (mark "working" until 2026-11-12); whether SpecialtyCare has opened a new IONM training cohort. From early November, name 2026-11-12 in the action list.
E. For every reachable row carry the NAME of the EEG/neurodiagnostic lab manager plus a department phone where the Indeed posting text or job_board_state.md already has it. A named human beats a posting URL — but do not web-search for one from here.

STEP 4 — Rewrite `claude/job_board_state.md`: refreshed tables, today as Last refresh, re-prioritised action list, and KEEP the "Rejected / already applied" and Retired tables. Prefer the shortest gate opening the NEAREST job over the shortest gate overall. Re-read the file immediately before writing it.

STEP 5 — Write `claude/next_rows.json`. This is the exact row set you will publish in STEP 8, kept as a file for the record. It must be valid JSON and nothing else — no prose, no code fence. Write it even if a step was skipped or a tool was unavailable, and say so in `note` in one calm clause. Shape:
{"generated":"YYYY-MM-DD","note":"one calm sentence, no guilt, no counts","action":"the single most useful thing she could do today, one sentence","rows":[ ... ]}
Each row:
  s        "open" | "working" | "gated"  REQUIRED. open = she can apply today. working = real but not reachable yet, including every internal Cleveland Clinic row before 2026-11-12. gated = needs a registry or accredited programme first.
  role, employer   strings
  where    string;  mi  number of miles from 33021, optional
  chips    [{t,k}] — k "key" for green, "wait" for amber, omit for plain
  lane     REQUIRED on every row. One of exactly these ids — the page groups and filters the board by it,
           and any row without one falls into a visible "Needs a lane" bucket:
           "eeg" | "ionm" | "psychometrist" | "sleep" | "ra" | "interpreter" | "tms" | "neurofeedback" | "scoring" | "biomarker" | "ketamine" | "crc"
           "crc" is the backup lane and the page marks it as such — use it only for coordinator roles, and never
           let it crowd out the psych/neuro lanes. If a row genuinely fits none of the twelve, omit lane rather
           than forcing one; the page handles that honestly.
  why      one or two plain sentences on why it is reachable or what stands in the way. When a row is imperfect (wrong shift, wrong county, adjacent field), say which part is imperfect in the row itself.
  gate     the employer's own requirement line, quoted, optional
  opens    dates and windows, optional
  contact  {name, phone}, optional but valuable
  url, link   the URL and the link's label
  fit      0-100 integer. REQUIRED on every actual job; OMIT on training programmes and credential paths, which are not applications. How far her resume gets sent exactly as it is, against THAT posting's stated requirements. A credential she does not hold caps this low however well the rest fits — that is the point of the number.
  ats      0-100 integer, same rule. How likely a screening system surfaces her in a recruiter's search: exact-title and exact-phrase overlap. Machine numbers run lower than human ones; do not inflate them to look encouraging.
  atsMax   0-100 integer, optional — where ats lands after the edit named in `fix`. Omit if the edit would not move it.
  fitWhy   one or two sentences: what the numbers are made of, naming the binding constraint.
  fix      the specific resume edit that would move `ats` for THIS posting — exact words to change, not general advice. HARD RULE: never propose adding a skill, tool, procedure or credential she has not actually done. If no wording moves it, say so and say what would (usually enrolment). A number raised by a false claim is worse than a low number; it collapses in the first conversation.
Order "open" rows first. At most 20 rows; if over, drop rows that are both under 60 fit and under 45 ats first, and never drop an "open" row. Rows under 60 fit AND under 45 ats are parked automatically by the page into a "Not worth your time yet" drawer — you do not filter them out, you score them honestly and let the page park them. Too few genuinely applicable jobs is the failure mode she has named; keep the ladder visible.

STEP 6 — Verify the file: `claude/next_rows.json` parses as JSON; every row has s/role/employer; every s is one of the three values; opens first; ≤20 rows; every actual job carries fit and ats; no `fix` invents experience she does not have. Fix and re-check on failure.

STEP 7 — Re-read the page IMMEDIATELY before building the edit (Artifact action "read" on the same url again). Build only from the file that read saves. The morning brief, the link checker and Stacey herself also write this page, so a stale base will be refused.

STEP 8 — Edit exactly one key. In the real `<script id="app-state">` JSON: replace ONLY `STATE.jobs.updated` (today), `STATE.jobs.note` (your file's `note`) and `STATE.jobs.rows` (your file's `rows`). Set `STATE.updated` to today.
- PRESERVE BYTE-FOR-BYTE: `STATE.jobs.docs` — her neurodiagnostic resume, full CV and BLS card as embedded PDFs plus preview images. Deleting it deletes her documents. It must hold exactly three entries with ids `resume`, `cv`, `bls`. Company-specific tailored resumes are never kept here; if one appears, something is wrong — stop and say so.
- PRESERVE BYTE-FOR-BYTE: `STATE.jobs.fitNote` and `STATE.jobs.searchTerms`.
- Touch NOTHING else. `pipeline`, `today`, `wins`, `checked`, `notes`, `display`, `phases`, `therapy`, `pinned` and `scheduled` are HERS. Do not touch the HTML, the styles or the scripts.

STEP 9 — Verify by RUNNING the checks in a script, not by reasoning about them: exactly one real app-state block; the JSON parses; `jobs.docs` has ids resume/cv/bls, each with non-empty `b64`, and is identical to what you read; `jobs.fitNote` and `jobs.searchTerms` identical to what you read; every top-level key other than `jobs` and `updated` byte-identical to what you read; only the app-state line differs from the source file; every row has a `lane` from the twelve ids. Fix and re-check on any failure. Do not publish a file that fails.

STEP 10 — Publish. The saved file starts with the platform's frame-runtime skeleton (`<!doctype html><html><head>…</head><body>`) and her content follows inside its body. Publish ONLY her content — everything after that skeleton's `<body>`, up to but not including the closing `</body></html>` — with no doctype, html, head or body tags of your own. Her content legitimately contains the string `<!doctype html>` inside a JavaScript template in `<script id="app-code">`; that is not a wrapper, leave it alone. Republish to that same url with label "jobs <date>". Do not pass `capabilities`. NEVER pass force. On a version conflict, re-read the newer version and redo the edit on top of it — someone else's work is in that version. If the read or the publish fails, do not force and do not create a new artifact; say in one line that the page could not be updated today and that the rows are in `claude/next_rows.json`.

STEP 11 — Append one dated line to `claude/sweep_log.md` (create it if absent): the date, whether you published, the row count, and anything that went wrong. Project docs have no append operation, so re-read the file and rewrite it whole. Keep it under 40 lines — drop the oldest entries. Do not rewrite `claude/debug.log`.

REPORT BACK in three lines or fewer, and make them count — this is the only message she gets from the daily search. Lead with the `action` field: ONE thing she could do today, never a list. No counts, no percentages, no guilt wording, no "overdue" or "carried over". If nothing reachable changed, say that plainly — a quiet day is a fine answer. If a tool was down, say which in five words, not a paragraph. She has ADHD and a long commute; a wall of text does not get read. Do not ask clarifying questions; this run is unattended.